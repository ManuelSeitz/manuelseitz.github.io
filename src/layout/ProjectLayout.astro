---
import { Font } from "astro:assets";
import "../styles/global.css";
import personal from "../data/personal.json";

interface Props {
  title: string;
  updatedDate: Date;
}

const { title, updatedDate } = Astro.props;
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Manuel Seitz | {title}</title>
    <Font cssVariable="--font-sans" preload />
  </head>
  <body class="overflow-x-hidden bg-neutral-950 font-sans text-neutral-50">
    <slot />
    <footer class="mt-8 border-t border-neutral-900 px-8 py-4 text-sm">
      <div class="mx-auto flex max-w-7xl items-center justify-between">
        <div
          class="flex items-center gap-2 max-md:flex-col max-md:items-start max-md:gap-0"
        >
          <strong>Manuel Seitz</strong>
          <div class="size-1 rounded-full bg-neutral-50 max-md:hidden"></div>
          <p class="text-neutral-400 max-md:text-xs">
            Actualizado el {
              updatedDate.toLocaleString("es", {
                dateStyle: "long",
              })
            }
          </p>
        </div>
        <div class="flex items-center gap-6">
          <ul class="flex items-center gap-2">
            {
              personal.social.map(({ name, href }) => (
                <li>
                  <a
                    href={href}
                    target="_blank"
                    rel="noopener noreferrer"
                    aria-label={name}
                  >
                    {name}
                  </a>
                </li>
              ))
            }
          </ul>
        </div>
      </div>
    </footer>
  </body>
</html>
